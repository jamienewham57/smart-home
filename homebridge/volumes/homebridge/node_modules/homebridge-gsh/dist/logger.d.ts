export declare class Log {
    private logger;
    private debugMode;
    constructor(logger: any, debugMode: boolean);
    debug(msg: any): void;
    info(msg: any): void;
    warn(msg: any): void;
    error(msg: any): void;
}
